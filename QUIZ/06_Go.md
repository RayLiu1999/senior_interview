# Go - 重點考題 (Quiz)

> 這份考題是從 Go 程式語言章節中挑選出**重要程度 4-5** 的核心題目，設計成自我測驗的形式。
> 
> **使用方式**：先嘗試自己回答問題，再展開「答案提示」核對重點，最後點擊連結查看完整解答。

---

## 🔄 並行模型 (Concurrency)

<a id="q1"></a>
### Q1: Goroutine 和 Thread 有什麼區別？
<!-- Concept ID: concept.go.concurrency.goroutine-scheduling; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐ (4) | **重要性**: 🔴 必考

請解釋 Goroutine 的工作原理，以及它與作業系統執行緒的差異。

<details>
<summary>💡 答案提示</summary>

| 特性 | Goroutine | OS Thread |
|------|-----------|-----------|
| **記憶體開銷** | ~2KB（可動態增長） | ~1-8MB（固定堆疊） |
| **創建成本** | 極低 | 高（系統調用） |
| **調度方式** | Go Runtime（用戶態） | OS Kernel（核心態） |
| **上下文切換** | 快速 | 較慢 |

**GMP 調度模型**：
- **G (Goroutine)**：執行的任務
- **M (Machine)**：作業系統執行緒
- **P (Processor)**：調度器（邏輯處理器）

**關鍵點**：
- Goroutine 是協作式調度
- Runtime 在特定時機（函數調用、Channel 操作等）插入調度點
- 可以輕鬆創建數萬個 Goroutine

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Concurrency/goroutine_vs_thread.md)

---

<a id="q2"></a>
### Q2: Buffered Channel 和 Unbuffered Channel 有什麼區別？
<!-- Concept ID: concept.go.concurrency.channel-buffering; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐ (5) | **重要性**: 🔴 必考

這是 Go 並行編程的核心概念，請解釋兩者的行為差異和適用場景。

<details>
<summary>💡 答案提示</summary>

| 特性 | Unbuffered | Buffered |
|------|------------|----------|
| **創建方式** | `make(chan T)` | `make(chan T, n)` |
| **發送阻塞** | 等待接收方 | 緩衝區滿時 |
| **接收阻塞** | 等待發送方 | 緩衝區空時 |
| **同步語義** | 同步通訊 | 異步通訊 |

**Unbuffered 用途**：
- 需要嚴格同步的場景
- 確保發送和接收同時發生
- 常用於信號傳遞

**Buffered 用途**：
- 生產者消費者模式
- 削峰填谷
- 避免 Goroutine 阻塞

**注意**：關閉已關閉的 Channel 會 panic！

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Concurrency/channel_buffered_vs_unbuffered.md)

---

<a id="q3"></a>
### Q3: Select 語句的工作原理和應用場景是什麼？
<!-- Concept ID: concept.go.concurrency.select-multiplexing; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐ (6) | **重要性**: 🔴 必考

請解釋 select 如何實現多路複用，以及常見的使用模式。

<details>
<summary>💡 答案提示</summary>

**Select 特性**：
- 同時監聽多個 Channel
- 隨機選擇一個可用的 case 執行
- 沒有可用 case 時阻塞（除非有 default）

**常見模式**：

**1. 超時控制**：
```go
select {
case result := <-ch:
    // 處理結果
case <-time.After(time.Second):
    // 超時處理
}
```

**2. 非阻塞操作**：
```go
select {
case ch <- data:
    // 發送成功
default:
    // Channel 滿，跳過
}
```

**3. 優雅退出**：
```go
select {
case <-done:
    return
case data := <-workCh:
    process(data)
}
```

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Concurrency/select_statement_and_use_cases.md)

---

<a id="q4"></a>
### Q4: Mutex 和 RWMutex 的區別是什麼？
<!-- Concept ID: concept.go.concurrency.read-write-locks; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐ (5) | **重要性**: 🟡 重要

請比較這兩種鎖的使用場景和效能特性。

<details>
<summary>💡 答案提示</summary>

| 特性 | Mutex | RWMutex |
|------|-------|---------|
| **鎖類型** | 互斥鎖 | 讀寫鎖 |
| **讀操作** | 互斥 | 可並行 |
| **寫操作** | 互斥 | 互斥 |
| **適用場景** | 讀寫均衡 | 讀多寫少 |

**RWMutex 方法**：
- `RLock()` / `RUnlock()`：讀鎖
- `Lock()` / `Unlock()`：寫鎖

**選擇指南**：
- 讀寫比例 > 10:1 → RWMutex
- 讀寫均衡 → Mutex
- 臨界區很小 → 可能 Mutex 更快

**注意事項**：
- 不要複製 Mutex
- 必須 Unlock 同一個鎖
- 避免死鎖（鎖順序一致）

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Concurrency/mutex_vs_rwmutex.md)

---

<a id="q5"></a>
### Q5: WaitGroup 如何正確使用？
<!-- Concept ID: concept.go.concurrency.waitgroup-synchronization; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐ (4) | **重要性**: 🟡 重要

請說明 WaitGroup 的用途和常見錯誤。

<details>
<summary>💡 答案提示</summary>

**WaitGroup 用途**：等待一組 Goroutine 完成

**三個方法**：
- `Add(n)`：增加計數器
- `Done()`：減少計數器（等同於 `Add(-1)`）
- `Wait()`：阻塞直到計數器歸零

**正確用法**：
```go
var wg sync.WaitGroup

for i := 0; i < 5; i++ {
    wg.Add(1)  // 在啟動 Goroutine 前調用
    go func() {
        defer wg.Done()
        // 工作...
    }()
}

wg.Wait()  // 等待所有完成
```

**常見錯誤**：
1. 在 Goroutine 內部調用 `Add()`
2. 忘記調用 `Done()`
3. `Add()` 和實際 Goroutine 數量不匹配
4. 計數器變成負數（panic）

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Concurrency/waitgroup_usage.md)

---

## ⚙️ 內部機制 (Internals)

<a id="q6"></a>
### Q6: Go 的垃圾回收機制是如何工作的？
<!-- Concept ID: concept.go.internals.concurrent-gc; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐⭐⭐ (8) | **重要性**: 🟡 重要

請解釋三色標記法和寫屏障的原理。

<details>
<summary>💡 答案提示</summary>

**三色標記法**：

| 顏色 | 含義 |
|------|------|
| **白色** | 未訪問，可能是垃圾 |
| **灰色** | 已訪問，子物件待掃描 |
| **黑色** | 已訪問，子物件已掃描 |

**流程**：
1. 初始：所有物件為白色
2. 標記：從根開始，灰 → 掃描子物件 → 黑
3. 清除：剩餘白色物件為垃圾

**寫屏障**：
- 解決並發標記時的物件丟失問題
- 當黑色物件引用白色物件時，將白色變灰

**GC 觸發條件**：
- 堆記憶體增長到閾值
- 手動 `runtime.GC()`
- 超過 2 分鐘未 GC

**優化建議**：
- 減少小物件分配
- 使用 sync.Pool
- 預分配 slice 容量

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Internals/go_garbage_collection.md)

---

<a id="q7"></a>
### Q7: Defer 的執行時機和順序是什麼？
<!-- Concept ID: concept.go.internals.defer-return-values; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐ (6) | **重要性**: 🟡 重要

請解釋 defer 的工作原理、執行順序，以及與 return 的關係。

<details>
<summary>💡 答案提示</summary>

**基本規則**：
1. defer 在函數返回**之前**執行
2. 多個 defer 按 **LIFO（後進先出）** 順序執行
3. defer 的參數在**聲明時**求值

**與 return 的關係**：
```go
func example() (result int) {
    defer func() { result++ }()
    return 0  // result 最終為 1
}
```

**執行順序**：
1. 計算返回值
2. 執行 defer
3. 返回

**常見用途**：
- 資源釋放（文件、鎖）
- panic 恢復
- 日誌記錄

**陷阱**：
```go
for i := 0; i < 3; i++ {
    defer fmt.Println(i)  // 輸出：2, 1, 0
}
```

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Internals/defer_execution.md)

---

<a id="q8"></a>
### Q8: Slice 和 Array 的區別是什麼？
<!-- Concept ID: concept.go.internals.slice-array-representation; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐ (4) | **重要性**: 🔴 必考

請解釋 Slice 的底層結構和擴容機制。

<details>
<summary>💡 答案提示</summary>

| 特性 | Array | Slice |
|------|-------|-------|
| **長度** | 固定 | 動態 |
| **傳遞方式** | 值傳遞（拷貝） | 引用傳遞 |
| **聲明** | `[5]int` | `[]int` |

**Slice 底層結構**：
```go
type slice struct {
    ptr *T     // 指向底層陣列
    len int    // 長度
    cap int    // 容量
}
```

**擴容規則（Go 1.18+）**：
- cap < 256：翻倍
- cap >= 256：增長 `(cap + 3*256) / 4`

**常見陷阱**：
```go
a := []int{1, 2, 3, 4, 5}
b := a[1:3]  // b 與 a 共用底層陣列
b[0] = 100   // a[1] 也變成 100！
```

**最佳實踐**：
- 使用 `make([]T, len, cap)` 預分配
- 需要獨立副本時使用 `copy()`

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Internals/slice_vs_array.md)

---

## 📦 標準庫 (Standard Library)

<a id="q9"></a>
### Q9: Context 套件如何使用？
<!-- Concept ID: concept.go.standard-library.context-cancellation; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐⭐ (7) | **重要性**: 🔴 必考

請解釋 Context 的設計理念、常用函數和使用規範。

<details>
<summary>💡 答案提示</summary>

**Context 用途**：
- 請求取消傳遞
- 超時控制
- 請求範圍內的值傳遞

**常用函數**：

| 函數 | 用途 |
|------|------|
| `context.Background()` | 根 Context |
| `context.WithCancel()` | 可取消的 Context |
| `context.WithTimeout()` | 帶超時的 Context |
| `context.WithDeadline()` | 帶截止時間的 Context |
| `context.WithValue()` | 傳遞請求範圍的值 |

**使用規範**：
1. Context 作為函數**第一個參數**
2. 不要將 Context 存在結構體中
3. 不要傳遞 nil Context
4. WithValue 只用於請求範圍的數據

**超時控制範例**：
```go
ctx, cancel := context.WithTimeout(context.Background(), time.Second)
defer cancel()

select {
case <-ctx.Done():
    return ctx.Err()  // 超時或取消
case result := <-ch:
    return result
}
```

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Standard_Library/context_package_usage.md)

---

## 🌐 Web 框架

<a id="q10"></a>
### Q10: Gin 框架的核心概念是什麼？
<!-- Concept ID: concept.go.gin.framework-architecture; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐ (4) | **重要性**: 🔴 必考

請說明 Gin 的路由機制、中間件和效能特點。

<details>
<summary>💡 答案提示</summary>

**Gin 特點**：
- 基於 **httprouter**，效能極高
- 路由使用 **Radix Tree**
- 零分配路由匹配

**核心組件**：

| 組件 | 說明 |
|------|------|
| `gin.Engine` | 路由引擎 |
| `gin.Context` | 請求上下文 |
| `gin.RouterGroup` | 路由分組 |
| `gin.HandlerFunc` | 處理函數 |

**路由參數**：
```go
r.GET("/user/:id", handler)    // 路徑參數
r.GET("/user/*action", handler) // 通配符
```

**參數綁定**：
```go
c.Bind(&obj)      // 自動選擇
c.BindJSON(&obj)  // JSON
c.BindQuery(&obj) // Query String
```

**響應方法**：
```go
c.JSON(200, gin.H{"key": "value"})
c.String(200, "Hello")
c.Redirect(302, "/new")
```

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Frameworks/Gin/gin_framework_basics.md)

---

<a id="q11"></a>
### Q11: 如何開發 Gin 中間件？
<!-- Concept ID: concept.go.gin.middleware-chain; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐ (6) | **重要性**: 🔴 必考

請解釋中間件的執行流程和常見的中間件類型。

<details>
<summary>💡 答案提示</summary>

**中間件本質**：`gin.HandlerFunc`

**執行流程**：
```
Request → Middleware1 → Middleware2 → Handler → Middleware2 → Middleware1 → Response
```

**c.Next() 的作用**：
- 執行後續的處理函數
- 形成「洋蔥模型」

**中間件範例**：
```go
func Logger() gin.HandlerFunc {
    return func(c *gin.Context) {
        start := time.Now()
        
        c.Next()  // 執行後續處理
        
        latency := time.Since(start)
        log.Printf("耗時: %v", latency)
    }
}
```

**常見中間件**：
- 日誌記錄
- 認證授權
- 錯誤恢復（Recovery）
- 限流
- CORS

**c.Abort() vs c.Next()**：
- `Abort()`：中止後續處理
- `Next()`：繼續執行

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Frameworks/Gin/gin_middleware_development.md)

---

<a id="q12"></a>
### Q12: Gin 有哪些效能優化技巧？
<!-- Concept ID: concept.go.gin.performance-optimization; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐⭐ (7) | **重要性**: 🟡 重要

請說明 Gin 應用的效能優化策略。

<details>
<summary>💡 答案提示</summary>

**1. 使用物件池**：
```go
var pool = sync.Pool{
    New: func() interface{} {
        return new(Response)
    },
}
```

**2. 避免不必要的序列化**：
- 使用 `c.Data()` 直接返回已序列化的數據
- 考慮使用 sonic 等高效 JSON 庫

**3. 路由優化**：
- 減少路由分組層級
- 避免過多的中間件

**4. Context 優化**：
- 使用 `c.Set()` / `c.Get()` 而非 Context.WithValue
- 避免在 Context 中存儲大物件

**5. 生產環境配置**：
```go
gin.SetMode(gin.ReleaseMode)
```

**6. 連接池設置**：
```go
srv := &http.Server{
    ReadTimeout:  5 * time.Second,
    WriteTimeout: 10 * time.Second,
    IdleTimeout:  120 * time.Second,
}
```

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Frameworks/Gin/gin_performance_best_practices.md)

---

## 🔬 進階主題

<a id="q13"></a>
### Q13: 如何避免 Goroutine 洩漏？
<!-- Concept ID: concept.go.concurrency.goroutine-scheduling; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐⭐ (7) | **重要性**: 🔴 必考

請說明 Goroutine 洩漏的常見原因和預防措施。

<details>
<summary>💡 答案提示</summary>

**常見洩漏原因**：

| 原因 | 說明 |
|------|------|
| Channel 阻塞 | 無人接收/發送 |
| 無限循環 | 沒有退出條件 |
| 等待永不發生的事件 | 死鎖 |
| Context 未取消 | 忘記調用 cancel |

**預防措施**：

**1. 使用 Context 控制生命週期**：
```go
ctx, cancel := context.WithCancel(context.Background())
defer cancel()

go func() {
    for {
        select {
        case <-ctx.Done():
            return  // 優雅退出
        case data := <-ch:
            process(data)
        }
    }
}()
```

**2. 確保 Channel 會被關閉**：
```go
go func() {
    defer close(ch)
    // 工作...
}()
```

**3. 使用 Buffered Channel 避免阻塞**

**4. 監控 Goroutine 數量**：
```go
runtime.NumGoroutine()
```

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Concurrency/goroutine_vs_thread.md)

---

<a id="q14"></a>
### Q14: Interface 的底層實現是什麼？
<!-- Concept ID: concept.go.internals.interface-representation; Learning Objective IDs: LO-1, LO-2, LO-3 -->

**難度**: ⭐⭐⭐⭐⭐⭐⭐ (7) | **重要性**: 🟡 重要

請解釋 Go interface 的內部結構和類型斷言的效能影響。

<details>
<summary>💡 答案提示</summary>

**Interface 底層結構**：

**eface（空介面）**：
```go
type eface struct {
    _type *_type  // 類型資訊
    data  unsafe.Pointer  // 資料指標
}
```

**iface（非空介面）**：
```go
type iface struct {
    tab  *itab    // 介面表
    data unsafe.Pointer  // 資料指標
}
```

**itab 結構**：
- 介面類型資訊
- 具體類型資訊
- 方法表（虛擬函數表）

**類型斷言**：
```go
// 安全斷言
v, ok := i.(Type)

// 不安全（可能 panic）
v := i.(Type)

// Type Switch
switch v := i.(type) {
case int:
    // ...
case string:
    // ...
}
```

**效能考量**：
- 類型斷言有執行時開銷
- 頻繁斷言時考慮使用泛型（Go 1.18+）

</details>

📖 [查看完整答案](../02_Backend_Development/Programming_Languages_and_Frameworks/Go/Internals/interface_internals.md)

---

## 📊 學習進度檢核

完成以上題目後，請自我評估：

| 評估項目 | 自評 |
|----------|------|
| 能解釋 Goroutine vs Thread | ⬜ |
| 理解 Buffered vs Unbuffered Channel | ⬜ |
| 能使用 Select 實現多路複用 | ⬜ |
| 能區分 Mutex 和 RWMutex | ⬜ |
| 正確使用 WaitGroup | ⬜ |
| 理解三色標記法 GC | ⬜ |
| 掌握 Defer 執行順序 | ⬜ |
| 理解 Slice 底層結構 | ⬜ |
| 能正確使用 Context | ⬜ |
| 熟悉 Gin 框架基礎 | ⬜ |
| 能開發 Gin 中間件 | ⬜ |
| 知道如何避免 Goroutine 洩漏 | ⬜ |

**建議**：未能完整回答的題目，請回到對應的詳細文章深入學習。
